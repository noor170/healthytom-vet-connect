/**
 * Realtime notifications placeholder.
 * With the Spring Boot backend, realtime notifications would need
 * WebSocket or SSE integration. This is a no-op stub for now.
 */
export function useRealtimeNotifications() {
  // No-op: realtime notifications require WebSocket/SSE from Spring Boot
}
